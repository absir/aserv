#options (
import = "org.jsoup.nodes.*"
)

${input.attr('type', 'checkbox')}
${input.removeClass('input-text')}
${input.removeAttr('value')}
##${input.addClass('ab_iCheck')}
${ScriptNode::attr(input, "${" + inputval+ " ? 'checked' : 'u'}", '1')}